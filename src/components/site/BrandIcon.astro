---
interface Props {
	path: string;
	class?: string;
}

const { path, class: className = "h-4 w-4" } = Astro.props;
---

<svg viewBox="0 0 24 24" class={className} fill="currentColor" xmlns="http://www.w3.org/2000/svg">
	<path d={path}></path>
</svg>
